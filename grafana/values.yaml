ingress:
  enabled: true
  ingressClassName: nginx
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
  hosts:
    - grafana.sevflow.app
  path: /
  pathType: Prefix
  tls: []

service:
  type: ClusterIP

adminUser: admin
